{
  "name": "miniproject_2k25",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\" \"npm run flask\"",
    "server": "cd backend && node Server.js",
    "client": "cd frontend && npm start",
    "flask": "cd backend && python app.py",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
